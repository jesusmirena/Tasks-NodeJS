doctype html
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1= pageName
            p Enter your email to retrieve your password 
           
                                
            form.caja-login(method="POST")
                .campo 
                    label(for="password") Password: 
                    input(type="password" name="password" placeholder="New password" value= password  required)
                .campo.enviar
                    input.boton(type="submit" value="Reset password")
                .campo.acciones