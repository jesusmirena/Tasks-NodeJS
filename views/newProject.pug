extends layout
include mixins/createProject

block content
    .contenido-principal 
        h1 #{pageName}

        if errors
            each error in errors 
                .alerta.error= error.texto
        +createProject(project)

        